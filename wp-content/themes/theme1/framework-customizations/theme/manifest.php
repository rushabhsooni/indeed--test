<?php

	if ( ! defined( 'FW' )) {
		exit;
	}

	$manifest = array();
	
	$manifest['requirements'] = array(

	);
	
	$manifest['supported_extensions'] = array(
	  'backups' 			=> array(),
	  'page-builder' 	=> array(),
	  'sidebars' 			=> array(),
	  'portfolio' 		=> array(),
	);